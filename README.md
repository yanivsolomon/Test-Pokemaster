# Test-Pokemaster

This file is for testing my own game (Pokemaster) using Python, Selenium Webdriver and Pytest framework. 
Tests included:
Title matching test (valid/invalid).
Logo color test (valid/invalid).
Logo size test (valid/invalid).
Background image URL test (valid/invalid).
Functional testing of the game and all of its levels (valid/invalid).
